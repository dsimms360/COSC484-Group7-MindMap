import dotenv from 'dotenv';
dotenv.config();

export const getClientID = (res, req) => {
    
}

export const createClientID = (res, req) => {
    
}

export const getTodolist = (res, req) => {
    
}

export const createTodolist = (res, req) => {
    
}

export const getNote = (res, req) => {
    
}

export const createNote = (res, req) => {
    
}

// export const getCalender = (res, req) => {
    
// }

// export const createCalender = (res, req) => {
    
// }

export const getQuicklink = (res, req) => {
    
}

export const createQuicklink = (res, req) => {
    
}

export const getEvent = (res, req) => {
    
}

export const createEvent = (res, req) => {
    
}
